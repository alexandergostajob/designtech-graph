# View app at:

https://alexandergostajob.github.io/DesignTech_graph/

This app is built on a template with a minimal setup to get React working in Vite with HMR and some ESLint rules.

For changes:

bash
git add .
git commit -m "Update X"
git push
npm run deploy